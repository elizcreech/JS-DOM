// Change the text of the h1 tag to "I have changed the text"

function changeText () {
    var element = document.getElementById("title")

    element.innerHTML = "I have changed the text"
}

changeText()
